import { CustomerPageResponse } from './customer-page-response';

describe('CustomerPageResponse', () => {
  it('should create an instance', () => {
    expect(new CustomerPageResponse()).toBeTruthy();
  });
});
